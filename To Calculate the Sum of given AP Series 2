/* Calculate the sum of the following AP Series:
S = 1 + 3 + 5 + 7 +........+ n      */

#include<stdio.h>
int main()
{
	int n,i,sum=0;
	printf("Enter the length:");
	scanf("%d",&n);
	i=1;
	while(i<=n)
	{
		sum=sum+i;
		i=i+2;
	}
	printf("The sum is=%d",sum);
	return 0;
}
