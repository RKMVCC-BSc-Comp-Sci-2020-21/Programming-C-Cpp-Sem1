//Calculate string length without using strlen
#include<iostream>
using namespace std;

int main()
{
    char str[100];
    int i;
    cout<<"Enter the stirng:";
    gets(str);
    cout<<"The string length is: ";
    for(i=0;str[i]!='\0';++i);
    cout<<i;
    return 0;
}
